apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-services.fullname" . }}
  labels:
    {{- include "user-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-services.selectorLabels" . | nindent 4 }}
